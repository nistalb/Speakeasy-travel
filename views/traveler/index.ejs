<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>you are on the index page</h1>
        
        <% if (!traveler.length) { %>
            <a href="/traveler/new">Enter New User Information</a>
        <% } else {%>
    
        <p><%= traveler[0].name %> </p>
        <p><%= traveler[0].bio %> </p>

        <a href="/traveler/<%= traveler[0]._id %>/edit ">Edit</a>
        <form action="/traveler/<%= traveler[0]._id %>?_method=DELETE" method=POST>
            <input type="submit" value="Delete">
        </form>
        
        <% } %> 
</body>
</html>